if (!head)

    //     if (head->next == head)
    //     {
    //         if (head->data == val)
    //         {
    //             delete head;
    //             head = nullptr;
    //             return;
    //         }
    //     }
    // return;

    // if (head->next == head && head->data == val)
    // {

    //     delete head;
    //     head = nullptr;
    //     return;
    // }